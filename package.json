{
  "name": "observer-pattern-ts",
  "version": "1.0.0",
  "description": "Implementation of pattern Oberserver in TS.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "create": "rimraf ./build && mkdir build && node_modules/pkg/lib-es5/bin.js dist/*.js --output build/index"
  },
  "keywords": [],
  "author": "emarifer",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.8.3",
    "nodemon": "^2.0.20",
    "pkg": "^5.8.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
